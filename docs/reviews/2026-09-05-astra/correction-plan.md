# Minor Revisions correction and restart plan

Proposed on 2026-09-05; Leonardo requested implementation on 2026-09-05. Correction preparation is authorized. Exact candidate rules return for the consolidated baseline decision. See baseline-candidate.md and validation.md for execution results.

Purpose: correct the September review findings, preserve the submitted Step-4 work, and resume development under an Astra-led workflow. The game remains a private local project. The complete game remains the target; Steps 1–3 retain their acceptance history.

## 1. Establish candidate correction directions

I recommend testing these three candidate design directions:

1. Keep the existing period indices 0–63 and final 06:42 boundary. Treat the available budget as 63 advances. Require a named honest core route in both profiles. Aim to make a named broader honest route feasible in Supported, with Standard requiring more sacrifice. Do not require every optional task and repeat to fit. Select exact cost/recovery changes only after a schedule analysis; show the smallest viable alternatives before approving new values.
2. Use an opening slice spanning the early weeks rather than forcing the full loop into Week 1. Test Weeks 1–3 as the initial candidate, preserving real action and energy rules. End after the selected loop and claim rehearsal; add no later experiment or scene merely because the clock advances. Define the rehearsal as an explicit evaluation-only action with its own stated cost, saved state, and completion condition. Verify the exact cutoff before freezing it.
3. Preserve the tone and claim ladder. Make honest claims depend on their scientific meaning. Prefer explicit question/coverage differences for experiment choices, or simplify options that have no useful trade-off. Do not add a resource system or a new experiment family to justify an existing menu option.

These are recommendations, not recorded creative decisions. Exact balance values, revised claim text, and significant dialogue changes return in one consolidated baseline proposal.

## 2. Preserve the handover and prepare simpler governance

Current inspected heads: main `9e3dba9`, rules `f2babe9`, combined diagnostic `f7192fa`. The latter contains the rules copies and the npm 12.0.2 amendment. It is unaccepted and does not include all later main history. The rules branch must not also be integrated as a second copy.

Preparation produces candidate records and diffs. Changes to governance authority take effect only after the consolidated decision described below.

Work:

- Recheck Git status and branch heads immediately before changes. Preserve existing branches and unrelated work.
- Create an isolated correction branch from current main for documentation and analysis work. Record exact references to the newer unintegrated packet without marking it accepted or copying its code to main.
- Store the review and a finding register with stable IDs, source, severity, proposed correction, affected contract, verification method, and status. Distinguish corrected documentation, analysis evidence, and later runtime proof.
- Make development-status.md the single concise current resume record, with accepted main state and newer branch work listed separately. Preserve historical evidence in the acceptance/contribution logs; replace repeated current-history prose elsewhere with links.
- Retain the already completed S13 heading correction in the newer packet. Do not repeat it from the older main snapshot. The current audit may still find other active-order inconsistencies.
- Update the agent model policy and re-review policy. Normalize future work orders with a clear distinction between primary work and delegated work. Keep stable historical records rather than reformatting the entire archive.

Prospective default routing, effective only after the approved governance commit and only for new or revised assignments. Historical work orders and contributions retain their actual model records:

| Role                           | Model and effort    | Responsibility                                                                                        |
| ------------------------------ | ------------------- | ----------------------------------------------------------------------------------------------------- |
| Primary                        | gpt-6-astra, high   | Design decisions, user discussion, all corrective-package tracked writes, tests, integration, records |
| Read-only evidence explorer    | gpt-5.6-terra, high | A bounded source, dependency, or asset-feasibility question                                           |
| Future implementation worker   | gpt-5.6-sol, high   | Complex implementation against an approved contract and exclusive paths                               |
| Independent technical reviewer | gpt-5.6-sol, xhigh  | Fresh-context review of the complete meaningful artifact                                              |

Routine future implementation may use Terra medium/high if the step plan gives a clear reason. Luna is optional for mechanical work, not a default extra agent. Keep at most two subagents concurrently and workers cannot delegate further. Role files and configuration must agree; custom role pins override spawn defaults. Record actual completed use rather than infer it from configuration. The present planning review uses the available Career Center roles, not the future game defaults.

Before complex implementation, the primary establishes that the contract represents complete valid examples and rejects specific counterexamples. The independent reviewer checks both contract conformance and game meaning. After a narrow correction, review that correction plus affected dependencies; repeat full review when shared behaviour or wider assumptions change. Mechanical record fixes get focused primary validation. A reviewer does not review its own implementation or accept a step.

## 3. Repair the calendar, balance, and slice contracts

Produce explicit schedules for:

- the honest core route in Standard and Supported;
- the proposed broader honest Supported route;
- a weakened-result route that reaches a valid ending;
- academic-only, industry-only, both-route, and no-route outcomes;
- the opening slice including queue response, breaks, monitoring, analysis, and rehearsal;
- the late-game tail and the final-period boundary.

Each trace states seed/outcome assumptions, period, energy, action, prerequisite, active experiments, monitoring, mandatory scene, expiry, room response, and expected state. Include normal and limited outcomes, conditional room costs, night surcharges, the five-energy cap, irreversible choices, and multi-period ordering.

Use a small non-production arithmetic checker for authored traces: clock advances, explicit action costs, energy debit/recovery, and the energy cap only. It must not implement route search, transitions, experiment resolution, or a second scheduler. Check windows, prerequisites, monitoring, scenes, expiry, room responses, and ordering manually against cited contracts. Label traces candidate until approved. Later runtime journey tests must reproduce accepted traces with real game code; arithmetic checks do not prove scheduler correctness or general route reachability.

Report exact feasible traces and failures. If a candidate does not fit, present the smallest options: change demand, recovery, a window, or the named route's optional workload. Do not silently change values. Keep Thin explicitly unreachable under the current starting evidence unless Leonardo selects a separate repricing change.

## 4. Repair experiment, manuscript, and text contracts

- Define the observations/readings required by Careful, Strong, and Inflated. Any approved experiment can supply a fact it actually establishes; do not require a named template without a scientific reason.
- Keep raw facts, reported facts, and what another character can see distinct. Honest association cannot establish cause. Deliberate false reporting remains possible under its existing explicit confirmation rules.
- Define one concrete trade-off or authored question difference for every voluntary risk/control option. Remove or constrain an option when it has no intended value. Compare alternatives under the same circumstances and seed assumptions.
- Replace result prose that assumes control or monitoring facts with fact-correct forms. Preserve the finite authored-text system.
- Correct the Samira authorship and unpublished-paper ending counterexamples, the Week-6 result message, profile warning text, and action-specific manuscript costs.
- Define semantic acceptance cases: each selected form must be true under every relevant selector condition. Check interactions among paper state, integrity, authorship, relationships, raw outcome, quality, and caveat. Neutral text can avoid unnecessary new branches.
- Make one focused editorial pass over character speech and branch endings. Present representative revised scenes and meaningful wording changes to Leonardo before freezing them. Preserve the existing fictional identity and the private human-review boundary.
- Add total authored words and representative per-run reading estimates. Keep scene and campaign duration as measured targets; player reading time is not a hard cinematic timer. Do not add filler to reach 90 or 180 minutes.

## 5. Move evidence earlier and freeze the corrected baseline

Amend the roadmap with stable supplemental checkpoint IDs rather than renumbering accepted history:

- `4A`: corrected contract and schedule-analysis gate, before accepting a revised Step-4 foundation.
- `6A`: machine-readable reference traces and semantic cases once the content/fixture foundations exist; absent runtime algorithms remain explicitly pending.
- `26A`: a temporary playable scenario combining a monitoring deadline, revision, and colleague/career opportunity; also a short late-tail test. State and rule prerequisites must exist before it runs. Move only the required subsets of later rules/content work into this checkpoint.
- Move whole-plan asset feasibility before expensive asset production and benchmark selection. It checks source routes, redistribution rights, adaptation effort, direct cost, and technical fit. It imports or purchases nothing.

The labels 4A, 6A, and 26A name supplemental gates attached to existing integer steps; they do not change the Step 0–70 identifier schema. These are internal evaluation scenarios, not extra shipped content. Keep normal player-facing acceptance at existing meaningful milestones. Exact production code paths for 6A/26A are defined in their later step plans once the revised contracts and dependencies exist.

Update every affected authoritative document, interface version, consumer, acceptance row, and open issue together. Preserve superseded decisions. Do not label an interface runtime-proven from a documentation review or checker result. Assess save compatibility explicitly; never silently discard local test saves.

Run one independent critical review on the complete revised baseline and evidence. Correct its findings and obtain Leonardo's decision on the consolidated rules/text proposal. The next exact code work order names the Step-4 amendment paths and tests; that approved baseline and work order authorize the dependent code correction.

## 6. Reconcile and complete Step 4, then resume the roadmap

After the consolidated decision, commit and validate the authoritative correction baseline, integrate the exact reviewed commits into local main, and record its new head. Build an explicit commit/path transplant map for the combined Step-4 packet against that head. Preserve both original Step-4 branches unchanged; do not merge the old packet wholesale or integrate the rules branch separately.

Prepare a new candidate branch from the corrected main baseline. Bring forward the required Step-4 source/package changes from the combined `f7192fa` packet exactly once, preserving contribution and approval history. Reconcile its control-document changes against the new baseline; do not blindly merge old current-status prose. Preserve the main S03 v4 heading correction and approved npm amendment.

Inspect and adapt the existing state types, validators, creation defaults, codec, and command shapes only where the approved baseline requires it. Add isolated positive/negative cases. Do not implement the full scheduler, manuscript engine, or campaign as part of Step 4.

Complete primary audit, independent review, applicable full verification, and controlled local diagnostic checks. Integrate only the reviewed candidate. Leonardo receives a short result/diagnostic packet and explicitly accepts Step 4. Then prepare the next step under the revised roadmap; prior approval does not automatically authorize all future steps.

## File and commit plan

Correction documentation and analysis scope:

- Add `docs/reviews/2026-09-05-astra/review.md`, `finding-register.md`, and `correction-plan.md`.
- Proposed explicit analysis-only exception to the current S12 fixture layout: add `docs/implementation/analysis/campaign-schedules.json`, `schedule-checker.mjs`, `schedule-checker.test.mjs`, and `README.md`. No new dependency or package-script change is planned. Run with Node and its built-in test runner. The source JSON records analysis assumptions and accepted/candidate labels; it does not compete with the later runtime catalogue.
  The following is a candidate impact map, not authorization to edit every listed file. Each final work order will give exact filenames and changes; remove a path if the evidence shows no dependency. Numbered shorthand refers to the existing named design document or Sxx specification.

| Finding                | Candidate authoritative files                                     | Intended change                                                                   |
| ---------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| F01 calendar           | design 03, 07, 13; S05, S12                                       | Correct spendable advances, named route requirements, boundary and schedule cases |
| F02 slice              | design 02, 12, 13, 15; S06, S12                                   | Explicit opening window, rehearsal costs/state and evaluation gate                |
| F03 scientific claims  | design 04, 12; S04, S06, S12                                      | Required facts, honest support and negative semantic cases                        |
| F04 experiment choices | design 04, 07, 12; S04, S06                                       | Approved question/coverage trade-offs and result consequences                     |
| F05 contradictory text | design 08, 10, 12; S06, S09, S12                                  | Fact-correct endings/results, profile wording and displayed costs                 |
| F06 early testing      | design 13, 14, 15; development-roadmap.md; S12                    | Supplemental evidence gates and explicit dependencies                             |
| F07 character voice    | design 05, 12                                                     | Approved speech distinctions and choice-responsive joins                          |
| F08 scope evidence     | design 13, 14                                                     | Authored/per-run reading estimates and early asset feasibility gate               |
| W01 governance         | AGENTS.md; S13; .codex/config.toml; affected .codex/agents/*.toml | Prospective routing, role boundaries and proportionate independent review         |

All Sxx paths are under `docs/implementation/specs/`. S03 and S07 change only if an approved correction affects state or saves; S01 only reconciles the already approved npm amendment. Design 01, 06, 09, 11 and unaffected role files are excluded unless the candidate dependency analysis demonstrates a required change.

Supporting record changes:

- `docs/00-design-index.md` and `docs/decision-log.md`: readiness and approved supersessions.
- `docs/reviews/recommendation-register.md`: link corrections to prior findings; preserve original reviews.
- `docs/glossary.md`: only changed terms.
- Implementation `development-status.md`: accepted head, pending packet, next action.
- Implementation `status.md`, `roadmap.md`, `decisions.md`, `interfaces.md`, `open-issues.md`, and `specification-audit.md`: only affected contract status, versions, consumers and remaining evidence gaps.
- Implementation `step-acceptance-log.md` and `ai-use-log.md`: actual acceptance and completed contributions only.
- Root `README.md`: replace duplicated current-state history with the authoritative resume link.
- New `.codex/agents/minor-revisions-evidence-explorer.toml`: bounded read-only game evidence role, subject to approved governance diff.
- Add the next unused work-order/contribution records for the correction packages after verifying the complete branch inventory. Preserve existing IDs.

Later Step-4 amendment candidates, to be finalized in its exact work order:

- `src/rules/campaign-state-types.ts`, `campaign-state-schema.ts`, `campaign-state.ts`, `campaign-state-codec.ts`, `command-contract.ts`, and `index.ts`;
- `tests/unit/MR-WP-01/campaign-state.test.ts`, `campaign-state-codec.test.ts`, `campaign-test-data.ts`, and `command-contract.test.ts`;
- existing diagnostic and related architecture/e2e files only if a changed public contract affects them; retain the package/foundation changes already present in the submitted packet.

No file deletion, published-history rewrite, public remote, push, licence creation, deployment, asset import, or Career Center update is included.

Proposed atomic commit messages:

1. `Record Minor Revisions review and correction programme`
2. `Update Minor Revisions agent workflow and resume records`
3. `Correct Minor Revisions calendar and slice contracts`
4. `Align Minor Revisions evidence claims and authored text`
5. `Add Minor Revisions early validation gates`
6. `MR-WP-01 Align Step 4 with corrected game contracts`
7. `Accept Minor Revisions corrected Step 4 foundation`

Commit 7 occurs only after actual acceptance. Material creative changes are committed as authoritative only after the consolidated baseline decision. Planning and candidate-analysis records can precede that decision with explicit status. All commits remain local.

## Validation and completion

- Check links, identifiers, exact counts, current/superseded state, work-order schema, TOML syntax/model routing, and complete diffs.
- Validate schedule arithmetic and boundary counterexamples using the analysis checker; review its assumptions independently.
- Check that text assumptions match selector facts, and that no honest claim acquires causal support.
- For code changes, run focused affected tests, `npm run check`, and `npm run verify` with the approved toolchain. Preserve coverage, browser, save/privacy, production-output, and no-network requirements applicable to Step 4. Repeat affected checks after repairs.
- Use a local game/diagnostic only under the explicit Step-4 test plan. Analysis and documentation work do not need a server.
- Report exact commits, tests performed, remaining unproved gameplay risks, and acceptance state. A failed required check blocks integration/acceptance.

Leonardo has requested the corrections. This authorizes preparation of the concrete correction package; there is no need to ask again whether to investigate and draft the fixes. The candidate directions above are not approved balance or creative decisions. Prepare the schedule evidence, proposed prose, exact governance diff, affected contracts, roadmap dependencies, and Step-4 work order before requesting one consolidated decision on the changed baseline. Until that decision, preserve proposals separately from authoritative frozen rules. Routine corrections within the approved scope need no repeated permission. Step-4 acceptance remains the final result decision.

## 2026-09-06 user amendment

Leonardo restricted Astra subagents to high-level work. Use Astra high/xhigh for design, architecture, shared-contract analysis and high-level review only. Delegated implementation uses Sol high/xhigh, Terra medium/high or Luna low/medium according to complexity. Detailed independent code review uses Sol xhigh; high-level design/governance review can use Astra xhigh. The generic reviewer profile leaves model/effort unset so the primary must select the correct assignment explicitly. This supersedes the earlier proposed Astra implementation-worker selection. The primary remains responsible for correction edits and integration.
